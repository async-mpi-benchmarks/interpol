[package]
name = "interpol-rs"
version = "0.1.0"
edition = "2021"

[build-dependencies]
cbindgen = "0.20.0"

[dependencies]
lazy_static = "1.4.0"
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"

[lib]
crate-type = ["cdylib"] # shared library (.so)
