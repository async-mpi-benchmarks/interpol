[package]
name = "interpol-rs"
version = "0.1.0"
edition = "2021"

[dependencies]
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
lazy_static = "1.4.0"

[lib]
crate-type = ["cdylib"] # shared library (.so)
